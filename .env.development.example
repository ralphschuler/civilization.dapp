# Use the same non-production password for the development app/migration job
# and PostgreSQL initialization. Replace this placeholder in the protected
# development environment; do not commit a real password.
PGPASSWORD=replace-with-dev-postgres-password
POSTGRES_PASSWORD=replace-with-dev-postgres-password
